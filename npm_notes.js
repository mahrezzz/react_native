npm install <package-name>   (--downlad a module)
npm install --save <package-name>    (--add this dependency to package.json of the project)
npm install --save-exact <package-name>@version    (--add this dependency to package.json of the project (strict to this version only))
npm install --save-dev  <package-name>   (add this package under the devDependencies)
